#include "Teacher.h"

Teacher::Teacher(string firstName, string lastName, string phoneNumber, string email, string password,
                 string gender)
    : Person(firstName, lastName, phoneNumber, email, password, gender) {
}

